# auth-local

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build auth-local` to build the library.

## Running unit tests

Run `nx test auth-local` to execute the unit tests via [Vitest](https://vitest.dev/).
