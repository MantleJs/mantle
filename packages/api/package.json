{
  "name": "api",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rimraf dist coverage tsconfig.tsbuildinfo",
    "compile": "tsc -b tsconfig.lib.json",
    "pretest": "yarn compile",
    "test": "jest --config=./jest.config.js"
  },
  "dependencies": {
    "@mantlejs/mantle": "^0.0.2"
  },
  "devDependencies": {
    "typescript": "^3.9.7"
  }
}